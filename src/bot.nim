# from env import loadEnv
# from tables import `[]`
import std/terminal

proc main() =
    # let
    #     info = loadEnv("src/.env")
    #     client = newClient(info["TOKEN"], info["USER_ID"], "digital_red_panda")
    # client.connectToChat()
    # client.joinChannel("digital_red_panda")
    # client.on(MessageEvent, 10)
    stdout.styledWriteLine({styleBlink}, "lmao")
    discard
main()
